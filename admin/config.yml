# Everything you need to know about configuring the CMS can be found here:-
# https://www.netlifycms.org/docs/configuration-options/

backend:
#  name: git-gateway # Netlify’s Git Gateway connects to Git provider’s API
#  branch: master # Branch to update (master by default)
  name: github
  repo: zakkolar/beta.zak.io

publish_mode: editorial_workflow


media_folder: "assets/images/uploads" # Folder where user uploaded files should go

#publish_mode: editorial_workflow # Enable drafts

collections: # A list of collections the CMS should be able to edit
# POSTS
- name: "post" # Used in routes, ie.: /admin/collections/:slug/edit
  label: "Post" # Used in the UI, ie.: "New Post"
  folder: "_posts" # The path to the folder where the documents are stored
  sort: "date:desc" # Default is title:asc
  create: true # Allow users to create new documents in this collection
  slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
  fields: # The fields each document in this collection have
  - {label: "Layout", name: "layout", widget: "hidden", default: "post"}
  - {label: "Title", name: "title", widget: "string", tagname: "h1"}
  - {label: "Publish Date", name: "date", widget: "datetime", format: "YYYY-MM-DD HH:MM:SS"}
  - {label: "Excerpt", name: "excerpt", widget: "text", required: false}
  - label: "Header"
    name: "header"
    widget: "object"
    fields:
      - {label: "Teaser image", name: "teaser", widget: "image", allow_multiple: "false", required: false}
      - {label: "Image", name: "image", widget: "image", allow_multiple: "false", required: false}
      - {label: "Image alt", name: "image_description", widget: "text", required: false}
      - {label: "Color", name: "color", widget: "text", required: false}
      - label: "Actions"
        name: "actions"
        widget: "list"
        required: false
        fields:
          - {label: "Label", name: "label", widget: "string"}
          - {label: "URL", name: "url", widget: "string"}
  - {label: "Categories", name: "categories", widget: "list", required: false}
  - {label: "Tags", name: "tags", widget: "list", required: false}
  - {label: "Body", name: "body", widget: "markdown", required: false}
  # PAGES
- name: "page"
  label: "Page"
  folder: "_pages"
  sort: "title:asc"
  slug: "{{slug}}"
  fields:
  - {label: "Layout", name: "layout", widget: "hidden", default: "page"}
  - {label: "Title", name: "title", widget: "string", tagname: "h1"}
  - {label: "Permalink", name: "permalink", widget: "string"}
  - {label: "Body", name: "body", widget: "markdown", required: false}